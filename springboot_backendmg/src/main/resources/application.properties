spring.application.name=springboot_backendmg
# parámetros de la conexión
spring.datasource.url=jdbc:mysql://localhost:3306/db_mundogolosin?serverTimezone=UTC&useSSL=false
spring.datasource.username=root
spring.datasource.password=

# Configuración de JPA
spring.jpa.hibernate.ddl-auto=update   
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Configuración de logs (opcional)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


# Establece el tamaño máximo por archivo a 10MB
spring.servlet.multipart.max-file-size=10MB

# Establece el tamaño máximo total de la petición a 10MB
spring.servlet.multipart.max-request-size=10MB